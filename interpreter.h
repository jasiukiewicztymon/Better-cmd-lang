#include <map>
#include <vector>
#include <string>

void interpret(
	std::string&,
	std::map<std::string, std::string>&,
	std::map<std::string, std::vector<std::string>>&,
	std::string&,
	int&,
	int&
);
