#include <string>
#include <vector>
#include <map>
#include <Windows.h>

void bcin(std::string&, std::string, std::map<std::string, std::string>, std::map<std::string, std::vector<std::string>>, std::vector<std::string>);
